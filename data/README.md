# Workshop data

Place these files on the self-hosted agent's persistent disk at:

`/mnt/ssw2026/project/data/`

Required files:

- `events_table.parquet`
- `stars_table.parquet`
- `sim_pops_table.parquet`

Do not commit large workshop datasets to Git unless their license and your repository policy permit it.
